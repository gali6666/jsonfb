---
description: jsonfb 前置沙箱（lib/sandbox）的测试工程标准（yalc 真实链接 + 真实远程服务 + 全覆盖零错误）
alwaysApply: true
---

# 前置沙箱测试标准（jsonfb 专属，强制）

本规则约束如何为 `lib/sandbox/` 编写与运行测试。沙箱本身的实现约束见 `front-sandbox.mdc`；本文件只管「怎么测」。任何测试改动都必须满足以下标准，否则视为不合格。

## 硬性约束（MUST）

1. **必须经 yalc 真实链接被测**：先在包根目录 `yalc publish`，再在消费端 `yalc add jsonfb && npm install`，最后 `npm test`。
   - **禁止**用相对路径 `require('../../lib/sandbox')`、`file:` 依赖或软链直接引用源码来「假装」测了发布产物。测试必须验证「发布出去、被别的项目装上后」是可用的。
2. **必须有真实远程代码服务（Express）**：测试依赖 `test/remote-mock-server/`，它是一个**基于 Express 真实启动**的远程服务（`test/remote-mock-server` 引入 `express` 作为依赖；被测包 `lib/sandbox` 仍保持 0 依赖，互不影响），提供：
   - `POST /v2/risk/get-risk-code`：带 `hash` 增量、返回 `{ data: { status, hash, riskCode(base64) } }`。
   - `POST /v2/risk/log`：接收 `remoteLog` 上报。
   - `POST /v1/risk/callback`：接收沙箱内远程代码的真实回调（用于证明代码确实执行）。
   - `__admin/*`、`/health`：测试用控制/观测端点。
   - 服务端必须做**真实 MD5 签名校验**（与 `lib/sandbox/sign.util.js` 同算法），独立实现、不反向 `require` 被测包。
   - 必须保留 `createServer()` 接口：返回 `{ server, app, state, listen, close, getSecret }`，可被 e2e 进程内 `createServer().listen(0)` 启动、亦可 `npm start` 独立运行。
3. **真实环境，不打桩**：不得 mock/stub `HttpClient`、网络层或 `vm`。HTTP 必须走真实 loopback socket，base64/签名/`vm` 执行全部真实发生。
4. **真实副作用断言**：端到端用例必须断言「远程 `init()` 在沙箱内执行后产生的真实副作用」（HTTP 回调或日志上报），而不仅是「代码被解码缓存」。
5. **依赖边界**：消费端测试**运行器**统一使用内置 `node:test` + `node:assert`，不得引入断言/测试框架类第三方依赖；被测包仅经 **yalc** 安装。**唯一允许的第三方依赖是远程测试服务 `test/remote-mock-server` 的 `express`**（真实远端框架，不属于被测包运行时）。被测包 `lib/sandbox` 自身仍严格 0 第三方依赖。
6. **零错误、零泄漏**：所有用例必须全绿；不得有未捕获异常、未关闭的 server、未清理的定时器。
   - 每个起服务的套件在 `after` 中关闭 server、调用 `stopRiskCodePolling()`。
   - 轮询定时器必须 `unref()`（实现侧已保证；测试侧需验证不会阻止退出）。

## 覆盖面（MUST 全覆盖）

- `sign.util.js`：`md5`（已知向量）、`simpleSortParams`/`recursiveSortParams`（忽略 `sign`、跳过空值、嵌套、bigint/数组/Decimal-like）、`signWithMD5`、`signWithHmacSha256`；并与服务端重算结果**交叉一致**。
- `http-client.js`：`post`/`get`/`put`、JSON 解析、非 2xx 拒绝（带 `statusCode`/`responseData`）、超时（`ECONNABORTED`）、**可重试错误的重试恢复**。
- `config.js`：`pickRandom`（恒在集合内 / 空数组 / 非数组 → `undefined`）、`getRemoteCodeUrl`/`getRemoteLogUrl`、`RISK_CODE_CONFIG` 形状与环境变量覆盖。
- **多地址（数组随机抽取 + 容灾）**：必须真正起**多个** mock 服务（本地多端口）验证 `remoteCodeUrls`/`remoteLogUrls` 为数组时的真实行为，而非仅对 `pickRandom` 做单元断言：
 - **随机分发**：多次 `fetchRemoteRiskCode` 后每个地址都被命中；全部在线时命中总数等于请求数（无丢失/无重试重复计数）。
 - **日志多地址**：多次 `remoteLog` 在多个日志地址间分发，且每条上报经服务端独立重算验签通过。
 - **故障转移**：数组中部分地址失效（关闭服务）时，拉取**绝不抛错**，存活地址仍能被随机选中并提供可用代码，失败命中累计到 `totalFailures`（可观测）。
- `SandboxManager`：`executeCode`（`main`/回退 `init`/都无 → `undefined`）、`executeInit`、缓存（`getCacheStats`/`clearCache`）、`setTimeout` 超限抛错、沙箱内原生 `require` 可用、注入的 `HttpClient`/`signWithMD5` 可用、远程改 `module.exports` 不污染包导出、两次执行上下文隔离。
- 端到端轮询：`fetchRemoteRiskCode` → `getRiskCode` → 沙箱 `init` 真实回调；同 `hash` 不更新；换版本后缓存清理并重新 `init`；`startRiskCodePolling`/`stopRiskCodePolling` 幂等与清理；`remoteLog` 真实上报。
- 健壮性：服务返回畸形/坏 base64/错误结构、下游不可用时，**绝不抛错**（`fetchRemoteRiskCode` 返回 `false`），require 被测包不崩溃。
- 导出契约：`require('jsonfb')` 为 `parse/stringify` 工厂且 **`.sandbox === undefined`**（主包不挂载沙箱句柄）；沙箱 API 只能经子路径 `require('jsonfb/lib/sandbox')` 取得且导出齐全。该契约必须由 `node:test` 锁定（不仅靠 `verify.sh`）。

## 配置时序（关键）

`config.js` 在 **require 时**读取 `process.env.RISK_CODE_URLS` / `REMOTE_LOG_URLS`。因此测试必须：

1. 先启动 mock 服务，拿到真实端口；
2. 设置 `process.env.RISK_CODE_URLS` / `REMOTE_LOG_URLS` 指向该服务（**经 env 注入配置，供测试使用导出**）；
3. **之后**再 `require('jsonfb/lib/sandbox')`。

顺序颠倒会导致沙箱连到默认地址，端到端用例失败。

> 注意：`require('jsonfb')` **不导出** `sandbox`（主包只 `require('./lib/sandbox')` 触发轮询副作用）。测试**必须**经子路径 `require('jsonfb/lib/sandbox')` 取得 `sandboxManager`/`fetchRemoteRiskCode`/`getRiskCode` 等导出，且该 require 必须发生在 env 设置**之后**。

## 发布前自检

- [ ] `package.json` 的 `files` 含 `lib/sandbox`，`yalc publish` 后消费端可经 `require('jsonfb/lib/sandbox')` 拿到沙箱 API；`require('jsonfb')` 的 `parse/stringify` 可用。
- [ ] `require('jsonfb')` **不对外导出 `sandbox`**：主包仅在加载时 `require('./lib/sandbox')` 触发副作用（轮询启动），不挂载 `.sandbox` 句柄。需要沙箱 API 时**必须显式**引入子路径 `require('jsonfb/lib/sandbox')`。
- [ ] 测试要拿到可用导出，**必须先经 env 注入配置（`RISK_CODE_URLS` / `REMOTE_LOG_URLS` 等）再 `require('jsonfb/lib/sandbox')`**（config.js 在 require 时读取环境变量，详见「配置时序」）。
- [ ] 远程服务（Express）可 `npm start` 独立运行，亦可被 e2e 进程内 `createServer()` 启动；其依赖 `express` 已随 `test/sandbox-e2e` 的 `setup`（或 `verify.sh`）安装。
- [ ] 所有用例零错误、无遗留 server/定时器/句柄。
- [ ] 除远程测试服务的 `express` 外，未引入其它第三方运行时依赖；被测包 `lib/sandbox` 仍 0 依赖。
